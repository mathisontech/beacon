import { TabPlaceholder } from "./tab-placeholder";

export function CommunityTab() {
  return (
    <TabPlaceholder
      checkpoint="Checkpoint 5"
      title="Community"
      subtitle="Contacts, groups, messaging, status updates from loved ones."
    />
  );
}
