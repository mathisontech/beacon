import { NationsPanel } from "./nations-panel";

export const metadata = {
  title: "Nations — Volcano Data Manager — Beacon Dev",
};

export default function NationsPage() {
  return <NationsPanel />;
}
