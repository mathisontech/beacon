import { SourceManagerPanel } from "../source-manager/source-manager-panel";

export const metadata = {
  title: "Data Source Manager — Beacon Dev",
};

export default function SourcesPage() {
  return <SourceManagerPanel />;
}
