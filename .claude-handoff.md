# handoff

Read this first after a context clear. Keep it short.

## repo
- path: `/sessions/eloquent-sweet-hopper/mnt/beacon`
- active branch: `beacon-client-v1`
- current head: `4e17de3` (C1.9a.3)
- parent of current head: `7ceac25` (C1.9a.2)
- remote push: user runs push manually, do not push

## project
Beacon. Disaster/hazard awareness. Work happens in `apps/beacon-dev` (Next.js 16 App Router).

## git is broken
`.git/index.lock` and `.git/HEAD.lock` are stuck and cannot be removed. Do NOT try `git add`, `git commit`, `git checkout`. Use the plumbing workflow:

1. `rm -f /tmp/beacon-idx && GIT_INDEX_FILE=/tmp/beacon-idx git read-tree <parent-sha>`
2. `GIT_INDEX_FILE=/tmp/beacon-idx git update-index --add --remove <paths...>`
3. `GIT_INDEX_FILE=/tmp/beacon-idx git diff --cached --stat <parent-sha>` — sanity check
4. `NEW_TREE=$(GIT_INDEX_FILE=/tmp/beacon-idx git write-tree)`
5. `GIT_AUTHOR_NAME="Kristin Mullaney" GIT_AUTHOR_EMAIL="kristin.mullaney@outlook.com" GIT_COMMITTER_NAME="Kristin Mullaney" GIT_COMMITTER_EMAIL="kristin.mullaney@outlook.com" git commit-tree "$NEW_TREE" -p <parent-sha> -m "..."`
6. Update ref via Write tool to `.git/refs/heads/beacon-client-v1` with the new commit sha + trailing newline.
7. `git log --oneline -1 beacon-client-v1` to verify.

`git status` lies because the index is stale. Trust `git ls-tree -r <sha>` for ground truth.

## file delete is broken
`rm` fails with "Operation not permitted". Call `mcp__cowork__allow_cowork_file_delete` with a sample path first, then `rm` works for the session.

## typecheck
- `cd apps/beacon-dev && npx tsc --noEmit 2>&1 | grep -c "error TS"` → baseline is 28
- Zero in changed files is the bar. Do not try to fix baseline unless asked.

## user preferences (global CLAUDE.md)
- Short code. Break into files with descriptive names. Functions folder with one function per file.
- Minimal text output. No icons. No emojis. Performance reports as terse as possible.

## feature in progress: volcano data manager
Sidebar path: Visualization Manager > Hazards > Volcanoes > {Map, Volcano Data Manager > {Data Source Manager, Nations}}.

Routes (all under `apps/beacon-dev/src/app/admin/dashboard/viz/hazards/volcanoes/`):
- `page.tsx` — renders `<VolcanoPlayground />` wrapped in `absolute inset-0` div
- `data-manager/layout.tsx` — shared chrome (back crumb to Volcanoes)
- `data-manager/page.tsx` — redirects to `/sources`
- `data-manager/sources/page.tsx` — renders `SourceManagerPanel`
- `data-manager/nations/page.tsx` — renders `NationsPanel`

Component files (not routes, don't rename):
- `data-manager/source-manager/source-manager-panel.tsx` — placeholder, C1.9b will build the coverage map
- `data-manager/nations/nations-panel.tsx` — nation left rail + `NationView`
- `data-manager/nations/nation-view.tsx` — volcano chip list, resets selection on nation change
- `data-manager/nations/volcano-view.tsx` — per-volcano in-page tabs (Data Pipeline / Current Overview). In-page tabs are OK here — 150+ volcanoes can't go in the sidebar.
- `data-manager/nations/volcano-pipeline.tsx`, `volcano-overview.tsx` — placeholders
- `volcano-country.ts` — `countryForRegion()` and `bucketVolcanoesByNation()`
- `data-manager/data-manager.css` — styles for vdm-* classes

Sidebar wiring lives in `apps/beacon-dev/src/app/admin/dashboard/layout.tsx`:
- State: `volcanoesNavOpen`, `volcanoDmNavOpen`
- Constants near top: `VOLCANOES_HREF`, `VOLCANO_DM_HREF`, `volcanoDmTabs`
- Special case inside the viz-group `group.items.map` that detects `item.href === VOLCANOES_HREF` and renders the nested subtree instead of a flat Link
- Auto-open useEffect branch checks `pathname?.startsWith(VOLCANOES_HREF)`

## next work (C1.9b and beyond — not started)
Data Source Manager needs to become interactive:
- Coverage polygons per region, colored by depth (high / partial / low / none)
- Per-region drawer listing data sources
- Per-source detail: polling limits, magnitude floor, sensor footprint, commercial-use license
- Ingestion health and stale-feed triggers

After that, the per-volcano pipeline + overview in `volcano-pipeline.tsx` / `volcano-overview.tsx` (detailed spec in the original request from the previous session — ingestion records, announcement log, warning signs, pattern-deviation, structural damage, magma chamber sims, live cams, population, satellite hotspots, event history, coverage depth indicator).

## ask before assuming
User was recently frustrated by me misinterpreting "subtabs" as in-page tabs when she meant sidebar nesting. If a restructure request is ambiguous, ask with AskUserQuestion before building.
